\documentclass[11pt]{article}

% ---------------------------------------------------------------------------
% Endpoint-precision note --- the two-scanner study: the distal-edge estimator
% and its precision on the two representative CRYSP configurations (BGO 195 K
% and cryogenic CsI, same ring, 2 X0 crystals). British spelling.
% Compile: tools/latex_compile.py endpoint_precision
% Figures from the output tree are collected by tools/collect_note_figures.sh
% ---------------------------------------------------------------------------

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[british]{babel}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{amsmath}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[hidelinks]{hyperref}
\usepackage{amssymb}

\sisetup{detect-all}
\newcommand{\Rfifty}{R_{50}}
\newcommand{\DR}{\Delta R}

\title{Precision of $\beta^+$ distal-edge verification:\\
  the two representative CRYSP scanners}
\author{J.J. G\'omez-Cadenas}
\date{\vspace{-3em}}

\begin{document}
\maketitle

\begin{abstract}
We quantify the precision with which an in-room PET scanner determines the
distal edge of a proton field from the $\beta^+$ activity it induces, for
the two representative CRYSP configurations: BGO at \SI{195}{K} and
cryogenic CsI, on the same closed \SI{1}{m} ring with two-radiation-length
crystals. Each scanner is simulated as ten independent \SI{1}{Gy} runs of
an SOBP field in a head phantom, reconstructed with the full list-mode
chain. The range observable is the midpoint $\Rfifty$ of the distal
activity fall-off, fitted on the whole-plane depth profile; its offset to
the dose edge is a calibration constant of each scanner, measured here to
\SIrange{0.01}{0.04}{mm} per term of the chain. At the working protocol
(all events, no corrections) both scanners reach the same precision,
$\sigma_R = \SI{0.11}{mm}$ per \SI{1}{Gy} run: BGO's $2.1\times$
statistics compensates its $2.1\times$ scatter fraction. On true
coincidences the scanners separate ($0.065$ vs.\ \SI{0.084}{mm}), which is
the margin a scatter correction would recover. The precision follows
$1/\sqrt{\text{dose}}$ with a dose-invariant calibration on both scanners,
so a \SI{0.1}{Gy} exploratory run locates the edge to
\SIrange{0.16}{0.23}{mm} before the clinical dose is delivered. The
precision also survives a realistic acquisition start: delaying to
\SI{180}{s} after beam-off halves the counts yet leaves
$\sigma_R \approx \SI{0.11}{mm}$, with the calibration constant walking
predictably at \SIrange{8}{11}{\micro m} per second of delay.
\end{abstract}

\section{Setup}
\label{sec:setup}

The study case is an SOBP proton field depositing a uniform \SI{1}{Gy} in a
head phantom: an ellipsoid of uniform brain tissue with semi-axes
$(72, 87, 102)$\,mm (the beam axis is $z$). The nuclear interactions of the
protons produce $\beta^+$ emitters along the beam path --- mainly $^{15}$O
($T_{1/2}=\SI{122}{s}$) and $^{11}$C (\SI{20.4}{min}), with $^{13}$N,
$^{10}$C and $^{14}$O contributions --- whose annihilation photons the
scanner images. Figure~\ref{fig:sobp} shows the two nominal curves of the
problem: the depth--dose $D(z)$ and the depth profile of the $\beta^+$
activity $A(z)$. Because isotope production requires protons above the
reaction thresholds ($\approx 17$ and \SI{18}{MeV} for the main $^{15}$O
and $^{11}$C channels), the activity ends about \SI{11}{mm} upstream of the
dose edge. The offset is a property of the beam and the tissue; the
reference simulation calibrates it.

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.46\textwidth]{figs/sobp_plateau.png}
  \includegraphics[width=0.46\textwidth]{figs/dose_activity.png}
  \caption{Left: the SOBP depth--dose and the definition of the clinical
    range point $R_{80}$, the depth at which the dose falls to $80\%$ of
    its plateau. Right: the nominal $\beta^+$ activity profile together
    with the dose profile; the activity edge sits $\approx \SI{11}{mm}$
    upstream of the dose edge, set by the production thresholds.}
  \label{fig:sobp}
\end{figure}

\subsection*{The two scanners}

Both configurations are the same closed cylindrical ring --- inner radius
\SI{387}{mm}, axial length \SI{1024}{mm}, $48\times20$ monolithic blocks
--- differing only in crystal, each at a depth of two radiation lengths
(the standard on which the CRYSP position resolution of \SI{3.5}{mm} FWHM
per axis is measured). Table~\ref{tab:arms} gives the detector constants
and the resulting per-run statistics; the timing windows come from the
calibrated CTR model of the production campaign, anchored to the cryogenic
CsI bench measurement of ref.~\cite{soleti_csi}.

\begin{table}[htb]
  \centering
  \caption{The two scanners. Shared: ring geometry, position resolution
    $\sigma_{xyz} = \SI{1.486}{mm}$ (\SI{3.5}{mm} FWHM), acquisition window
    \SIrange{0}{1200}{s}, and the annihilation sets (run $R$ sees identical
    decays on both scanners --- differences are detector-only). Statistics
    are per \SI{1}{Gy} run ($8.02\times10^{7}$ decays).}
  \label{tab:arms}
  \vspace{1mm}
  \begin{tabular}{lcc}
    \toprule
     & BGO (\SI{195}{K}) & CsI (${\sim}\SI{100}{K}$) \\
    \midrule
    crystal depth ($2X_0$)            & \SI{22.4}{mm} & \SI{37.2}{mm} \\
    energy resolution (FWHM, 511 keV) & 15\%          & 6\%  \\
    photopeak cut ($511 - 3\sigma_E$) & \SI{413}{keV} & \SI{472}{keV} \\
    coincidence window $\tau$         & \SI{5}{ns}    & \SI{1.5}{ns} \\
    \midrule
    coincidences                      & $15.3\times10^{6}$ & $6.1\times10^{6}$ \\
    true / scatter / random           & $72.3\,/\,27.4\,/\,0.3\%$
                                      & $86.6\,/\,13.3\,/\,0.1\%$ \\
    \bottomrule
  \end{tabular}
\end{table}

The pair spans the design trade-off: BGO records $2.1\times$ the true
coincidences; CsI records a purer sample --- half the scatter fraction and
$3\times$ fewer randoms, from its energy resolution and timing. The range
measurement quantifies the balance.

Images are reconstructed with list-mode MLEM~\cite{shepp_vardi} on a
$64\times64\times96$ grid of \SI{1.5}{mm} voxels covering the activity
corridor, with 50 iterations, per-event attenuation factors computed
analytically on the phantom ellipsoid, and a per-scanner sensitivity image
from $10^{9}$ sampled lines of response. Figure~\ref{fig:projections}
shows the orthogonal projections of one reconstructed BGO run; the CsI
image is identical in geometry, with $2.1\times$ fewer counts.

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.55\textwidth]{figs/phantom.png}\\[2mm]
  \includegraphics[width=\textwidth]{figs/recon_projections_bgo.png}
  \caption{Top: the head phantom, an ellipsoid of uniform brain tissue.
    Bottom: orthogonal projections of the reconstructed activity of one
    \SI{1}{Gy} BGO run (transaxial, coronal, sagittal). The dashed curve
    is the phantom outline; the dotted line marks the fitted $\Rfifty$.}
  \label{fig:projections}
\end{figure}

\section{The estimator}
\label{sec:estimator}

The depth profile is the activity integrated over the full transverse
plane at each depth,
\begin{equation}
  P(z) \;=\; \iint A(x,y,z)\,\mathrm{d}x\,\mathrm{d}y ,
  \label{eq:profile}
\end{equation}
which makes the profile insensitive to the transverse spreading of the beam
with depth. The distal fall-off is fitted with a complementary error
function plus a baseline,
\begin{equation}
  P(z) \;=\; b + \frac{a}{2}\,
  \operatorname{erfc}\!\left(\frac{z - z_0}{\sqrt{2}\,w}\right),
  \label{eq:erfc}
\end{equation}
inside a fixed window bracketing the edge ($-36.5 \le z \le \SI{-1.5}{mm}$
for the nominal beam), with Poisson weights. The four parameters are the
baseline $b$, the plateau amplitude $a$, the midpoint $z_0$ and the edge
width $w$.

\paragraph{Range points.} For any curve with a plateau and a distal
fall-off, $R_X$ denotes the depth at which the curve has dropped to $X\%$
of its plateau. Two of them play distinct roles:
\begin{itemize}
\item $\Rfifty \equiv z_0$, the \emph{midpoint} of the fall-off --- the
  depth at which the profile crosses half its plateau above baseline
  (Fig.~\ref{fig:fits}) --- is the range observable. It is where the
  profile is steepest, so it carries the smallest statistical error of any
  level, and Eq.~(\ref{eq:erfc}) yields it directly as a fit parameter.
\item $R_{80}$ of the \emph{dose} is the clinical prescription point
  (Fig.~\ref{fig:sobp}, left). For a given beam it differs from the dose
  $\Rfifty$ by a fixed geometric constant of the SOBP shape
  ($R_{80}-\Rfifty^{\text{dose}} = \SI{-2.0}{mm}$ for the nominal beam), so
  a measurement expressed in $\Rfifty$ converts to $R_{80}$ exactly.
\end{itemize}

\paragraph{The $\DR$ estimator.} The measured quantity is
\begin{equation}
  \DR \;=\; \Rfifty^{\text{activity}} - \Rfifty^{\text{dose}},
  \label{eq:dr}
\end{equation}
with the same fit construction, Eq.~(\ref{eq:erfc}), applied to both
curves. $\DR$ is calibrated once on the reference simulation of the
treatment plan --- per scanner, since the constant is a property of the
full chain; a range deviation then appears as the difference between the
measured $\Rfifty^{\text{activity}}$ and its calibrated expectation, and
every constant of the convention cancels. For the nominal beam the
truth-level value is $\DR = \SI{-10.74}{mm}$
($\Rfifty^{\text{activity}} = \SI{-14.32 \pm 0.01}{mm}$,
$\Rfifty^{\text{dose}} = \SI{-3.58 \pm 0.03}{mm}$).

The fitted edge shape is a superposition: each isotope's activity
terminates where the proton energy crosses its production threshold, so
the fall-off is a sum of edges at slightly different depths --- dominated
by $^{15}$O and $^{11}$C, with a deeper-reaching $^{13}$N foot from its
low ($\approx\SI{5.5}{MeV}$) threshold. Equation~(\ref{eq:erfc}) is a
convention whose parameters are validated by their stability across the
measurement chain and the dose range (Secs.~\ref{sec:budget}
and~\ref{sec:dose}); an isotope-resolved composite fit is a natural
refinement (Sec.~\ref{sec:outlook}).

Figure~\ref{fig:fits} shows the fit on one run of each scanner, at the
working protocol (all recorded events, no corrections).

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.49\textwidth]{figs/fit_all_events_bgo.png}
  \includegraphics[width=0.49\textwidth]{figs/fit_all_events_csi.png}
  \caption{Whole-plane depth profile of one reconstructed \SI{1}{Gy} run
    (points, Poisson errors) at the working protocol --- all events, no
    corrections --- with the erfc fit (Eq.~\ref{eq:erfc}, red line) in the
    fixed window (shaded). Left: BGO 195\,K; right: CsI. The dashed guides
    mark the fitted plateau and its half; $\Rfifty$ is the depth at which
    the fit crosses the half-height (dotted line, value in the legend).}
  \label{fig:fits}
\end{figure}

\section{Calibration budgets}
\label{sec:budget}

Each level of the measurement chain shifts $\DR$ by a definite, resolvable
amount, and the decomposition is measured per scanner over its ten
independent runs (Table~\ref{tab:ladder}): from the true activity to the
origins of the detected true coincidences (the acceptance level:
attenuation and solid-angle weighting), to the MLEM reconstruction of the
trues, to the reconstruction of \emph{all} events with no scatter or
randoms correction. Figure~\ref{fig:ladder} shows what survives the
calibration: the run-to-run reproducibility of $R$ at each level.

\begin{table}[htb]
  \centering
  \caption{The calibration budgets: $\DR$ at each level of the measurement
    chain (erfc fit), over each scanner's ten \SI{1}{Gy} runs --- mean
    $\pm$ run-to-run spread $\sigma$. The truth-level value, common to
    both, is \SI{-10.743}{mm}.}
  \label{tab:ladder}
  \vspace{1mm}
  \begin{tabular}{lll}
    \toprule
    level & {BGO 195\,K [mm]} & {CsI [mm]} \\
    \midrule
    detected trues (origins) & $-10.938 \pm 0.023$
                             & $-10.962 \pm 0.039$ \\
    reconstruction (trues)   & $-11.165 \pm 0.065$
                             & $-11.232 \pm 0.084$ \\
    reconstruction (all events, uncorrected)
                             & $-11.144 \pm 0.113$
                             & $-11.192 \pm 0.114$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.49\textwidth]{figs/ladder_delta_r50_bgo.png}
  \includegraphics[width=0.49\textwidth]{figs/ladder_delta_r50_csi.png}
  \caption{Run-to-run reproducibility of $R$ at each level of the
    measurement chain: the ten runs (circles), each level's own mean
    subtracted, with the mean $\pm$ run-to-run spread $\sigma$ (red bars).
    The level-to-level offsets are calibration constants absorbed by the
    per-scanner reference simulation (Table~\ref{tab:ladder}) and carry no
    precision information, so they are removed from the display. Left:
    BGO 195\,K; right: CsI.}
  \label{fig:ladder}
\end{figure}

The budgets read the same on both scanners: the acceptance level moves the
edge by about \SI{-0.2}{mm}, the reconstruction by a further
\SI{-0.25}{mm}, and admitting the scattered and random coincidences
uncorrected changes it by $+0.021$ (BGO) and $+\SI{0.040}{mm}$ (CsI). Each
term is a constant of the chain that the per-scanner reference simulation
calibrates; over the ten runs its mean is determined to
$\sigma/\sqrt{10}$, i.e.\ \SIrange{0.01}{0.04}{mm} --- well below the
effects the verification aims to detect.

The precision of the measurement is quoted as $\sigma$, the spread over
the ten independent runs: the fluctuation a repeated measurement has.

\section{Comparison of the two scanners at \texorpdfstring{\SI{1}{Gy}}{1 Gy}}
\label{sec:tie}

Table~\ref{tab:k} is the central result. On true coincidences BGO's
$2.1\times$ statistics gives the expected $\sqrt{N}$ advantage. At the
working protocol --- all recorded events, no corrections --- the scattered
coincidences widen the spread by a factor $1.7$ on BGO ($27.4\%$ scatter)
and $1.4$ on CsI ($13.3\%$), and the two scanners reach the same
precision:
\begin{equation}
  \sigma_R(\SI{1}{Gy},\ \text{all events})
  \;=\; 0.113 \pm 0.027\ \text{(BGO 195\,K)}
  \quad\text{vs.}\quad 0.114 \pm \SI{0.029}{mm}\ \text{(CsI)}.
\end{equation}

\begin{table}[htb]
  \centering
  \caption{$\sigma_R$ at \SI{1}{Gy} from each scanner's ten runs (erfc,
    whole-plane). The all-events row is the working protocol; the trues
    row is the precision a perfect scatter correction would recover.}
  \label{tab:k}
  \vspace{1mm}
  \begin{tabular}{lcc}
    \toprule
    selection & BGO 195\,K & CsI \\
    \midrule
    true coincidences  & $\SI{0.065 \pm 0.015}{mm}$ & $\SI{0.084 \pm 0.020}{mm}$ \\
    all events, uncorrected & $\SI{0.113 \pm 0.027}{mm}$ & $\SI{0.114 \pm 0.029}{mm}$ \\
    \bottomrule
  \end{tabular}
\end{table}

The scattered coincidences do not bias the edge: the shifts in
Table~\ref{tab:ladder} are \SIrange{0.02}{0.04}{mm}, because the scatter
background reconstructs into a smooth dome under the edge
(Fig.~\ref{fig:scatters}) whose window slope the free baseline of
Eq.~(\ref{eq:erfc}) absorbs (worst-case bounds $-0.13$ and \SI{-0.28}{mm};
measured $+0.02$ and $+\SI{0.04}{mm}$). Their cost is statistical:
mispositioned events fluctuate run to run without sharpening the edge, so
they add variance. Purity and statistics are exchangeable at this
protocol, and the two crystals exchange them at par.

A scatter correction therefore decides the comparison: recovering the
trues-level precision is worth a factor $1.7$ to BGO and $1.4$ to CsI
(Table~\ref{tab:k}). The correction --- or a tighter selection exploiting
CsI's energy resolution --- is the next step of the technology comparison.

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.49\textwidth]{figs/scatters_profile_bgo.png}
  \includegraphics[width=0.49\textwidth]{figs/scatters_profile_csi.png}
  \caption{The scatter background in context, both panels in the same
    units and on the same logarithmic scale --- fractions of each
    scanner's all-events plateau: the all-events profile the fit runs on
    (blue) and the scatters-only contribution to it (black), for one run. BGO 195\,K (left, $27.4\%$ of
    events) carries a $7\%$ pedestal under its edge, CsI (right, $13.3\%$)
    a $5\%$ one --- the amounts behind the variance factors $1.7$ and
    $1.4$ of Table~\ref{tab:k}. The pedestal's flat part is absorbed
    exactly by the free baseline of Eq.~(\ref{eq:erfc}); the residual
    tilt inside the window (red line) bounds the worst-case $\Rfifty$
    shift at $-0.13$ and \SI{-0.28}{mm}, and the measured shifts are
    $+0.02$ and $+\SI{0.04}{mm}$.}
  \label{fig:scatters}
\end{figure}

\section{Low-dose operation}
\label{sec:dose}

The dose axis is explored by thinning each stored run with the appropriate
Bernoulli probability and reconstructing each realisation with the
identical chain. Figure~\ref{fig:dose} shows every realisation's $R$
relative to the \SI{1}{Gy} mean, grouped by dose, and the measured
$\sigma_R$ against the $1/\sqrt{\text{dose}}$ law anchored at \SI{1}{Gy};
Table~\ref{tab:dose} quantifies the two operating points per scanner
(true coincidences).

\begin{table}[htb]
  \centering
  \caption{The two operating points per scanner (erfc fit, whole-plane
    profile, trues). $n$ is the number of independent realisations.}
  \label{tab:dose}
  \vspace{1mm}
  \begin{tabular}{llS[table-format=2]S[table-format=-2.3]S[table-format=1.3]S[table-format=1.3]}
    \toprule
    scanner & {dose [Gy]} & {$n$} & {$\DR$ [mm]} & {$\sigma_R$ [mm]}
      & {$\pm$ [mm]} \\
    \midrule
    BGO 195\,K & 1.0 & 10 & -11.165 & 0.065 & 0.015 \\
    BGO 195\,K & 0.1 & 20 & -11.192 & 0.159 & 0.026 \\
    CsI        & 1.0 & 10 & -11.232 & 0.084 & 0.020 \\
    CsI        & 0.1 & 20 & -11.198 & 0.230 & 0.037 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.49\textwidth]{figs/dose_sweep_r50_bgo.png}
  \includegraphics[width=0.49\textwidth]{figs/dose_sweep_r50_csi.png}
  \caption{Top panels: every realisation's $R$ relative to the \SI{1}{Gy}
    mean (circles), grouped by dose ($n = 10, 10, 20, 20$ at
    $1, 0.5, 0.2, \SI{0.1}{Gy}$), with mean $\pm$ spread $\sigma_R$ ---
    the calibration constant itself lives in Table~\ref{tab:k}; the group
    means agree with the \SI{1}{Gy} anchor within the spread, so the
    calibration is dose-invariant.
    Bottom panels: the spread $\sigma_R$ per dose against the
    $1/\sqrt{\text{dose}}$ prediction anchored at \SI{1}{Gy} (red line).
    Left: BGO 195\,K; right: CsI.}
  \label{fig:dose}
\end{figure}

Two facts hold on both scanners. The calibration is dose-invariant: the
group means agree with the \SI{1}{Gy} anchor well within the spread
down to \SI{0.1}{Gy} --- the reconstruction places the edge at the same
depth over a factor ten in statistics. And the precision is purely
statistical: $\sigma_R \propto 1/\sqrt{\text{dose}}$ at every measured
point, with no onset of fit instability at low counts.

This permits an \emph{exploratory dose} mode of operation. A \SI{0.1}{Gy}
run --- a small fraction of a \SIrange{1}{2}{Gy} daily fraction ---
locates the distal edge with $\sigma_R = 0.16$ (BGO) to \SI{0.23}{mm}
(CsI) and a calibration known to better than \SI{0.05}{mm}, an order of
magnitude below the millimetre scale that drives clinical
margins~\cite{paganetti2012}. The beam range can therefore be verified
against the plan before the therapeutic dose is delivered, with the same
scanner and the same analysis.

\section{Acquisition start time}
\label{sec:tstart}

An in-room scanner starts recording one to a few minutes after beam-off,
not at $t=0$: the results above are therefore the zero-delay limit, and
the start time is the axis that connects them to a realistic measurement.
The production files carry the absolute decay time of every coincidence,
so a delayed start is the pure event selection
$t_{\text{decay}} \ge t_{\text{start}}$ on the stored runs ---
statistically exact, with the per-isotope decay laws and the randoms' time
structure included (Fig.~\ref{fig:decays} shows the isotope activities
that drive it). Table~\ref{tab:tstart} and Fig.~\ref{fig:tstart} give
$\DR$ and $\sigma_R$ at $t_{\text{start}} = 60$, $120$, $180$ and
\SI{300}{s}, at the working protocol on both scanners.

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/activity_time.png}
  \caption{Activity of the $\beta^+$ emitters produced by the field, as a
    function of time after the beam turns off, with the acquisition window
    shaded. Built from the per-isotope decay counts and the physical
    half-lives (\texttt{tools/plot\_activity\_time.py}).}
  \label{fig:decays}
\end{figure}

\begin{table}[htb]
  \centering
  \caption{The acquisition-start axis (all events, erfc, whole-plane):
    the fraction of recorded events surviving the selection --- identical
    on both scanners, since the cut acts on the shared decay times --- and
    $\DR$ (mean $\pm$ run-to-run spread $\sigma$) per scanner.}
  \label{tab:tstart}
  \vspace{1mm}
  \begin{tabular}{S[table-format=3]S[table-format=3]ll}
    \toprule
    {$t_{\text{start}}$ [s]} & {kept [\%]} & {BGO 195\,K [mm]} & {CsI [mm]} \\
    \midrule
      0 & 100 & $-11.144 \pm 0.113$ & $-11.192 \pm 0.114$ \\
     60 &  77 & $-11.580 \pm 0.102$ & $-11.625 \pm 0.057$ \\
    120 &  61 & $-12.131 \pm 0.116$ & $-12.167 \pm 0.091$ \\
    180 &  48 & $-12.725 \pm 0.108$ & $-12.809 \pm 0.112$ \\
    300 &  32 & $-14.084 \pm 0.104$ & $-14.198 \pm 0.233$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp!]
  \centering
  \includegraphics[width=0.8\textwidth]{figs/tstart_r50.png}
  \caption{The acquisition-start trade-off (all events). Top: $\DR$
    (points, $\pm$ run-to-run spread $\sigma_R$) against the start time;
    the thin lines connect the points. Unlike the static calibration
    constants of Table~\ref{tab:ladder}, this shift is \emph{physical} ---
    the $^{15}$O component decays out of the isotope mix --- and its slope
    (\SIrange{8}{11}{\micro m/s}) sets how accurately the start time must
    be known. Bottom: $\sigma_R$ (points, with their uncertainty); the two
    scanners are offset by a few seconds for legibility.}
  \label{fig:tstart}
\end{figure}

Three facts emerge. First, \textbf{the precision survives the realistic
start}: at $t_{\text{start}} = \SI{180}{s}$ half the counts are gone,
yet $\sigma_R$ remains at $\approx\SI{0.11}{mm}$ on both scanners ---
at or below the counting expectation
$\sigma_R(0)/\sqrt{\text{kept fraction}}$ at every point
(\SI{0.16}{mm} at \SI{180}{s}). The delay removes a variance source
along with the counts: the $^{15}$O amplitude, which owns the deepest
part of the composite edge, fluctuates run to run, and it drains faster
than the statistics. On CsI the removal transiently over-compensates
($\sigma_R = \SI{0.057}{mm}$ at \SI{60}{s}, a $2\sigma$ dip at ten runs)
before the count loss restores the zero-delay level at \SI{180}{s} and
overtakes it at \SI{300}{s} ($\SI{0.233}{mm}$, at the counting
expectation --- the compensation is exhausted); BGO, with $2.5\times$
the counts, still reads \SI{0.104}{mm} there. The zero-delay precision
is therefore not an idealisation: an in-room scanner starting
\SIrange{2}{3}{min} after beam-off delivers it, and BGO holds it out to
five minutes. Second, the calibration
constant walks with the start time --- $-0.44$, $-0.99$, $-1.6$ and
\SI{-2.9}{mm} at $60$, $120$, $180$ and \SI{300}{s}, identically on both
scanners and at every level of the chain --- as the $^{15}$O component
(deeper edge, from its lower production threshold) decays out of the mix.
Calibrated at the operating start time this is bookkeeping; operationally
it sets a sensitivity of \SIrange{8}{11}{\micro m} per second of start
time, so timing the acquisition start to one second keeps it at the
\SI{0.01}{mm} level. Third, the fitted edge width grows mildly with the
delay ($10.8 \to \SI{11.4}{mm}$ on trues): the smaller positron range of
the surviving $^{11}$C is invisible under the intrinsic edge width, while
the deeper-reaching $^{13}$N component gains relative weight and broadens
the composite fall-off.

\section{Isotope washout}
\label{sec:washout}

The activity used so far is physical-decay-only: the $\beta^+$ emitters are
taken to sit where they are produced until they decay. In tissue they do not
--- perfusion and metabolism clear the radioactive atoms from the irradiated
volume before they decay (isotope washout). This is the one piece of
simulation-to-patient physics missing from the productions, and it acts on the
same isotope--time structure as the acquisition delay of the previous section.

We model washout as loss, with the three-exponential brain clearance of
Mizuno et al.~\cite{Mizuno2003}. For a spatially uniform clearance the effect
reduces to a per-isotope survival factor $g_i$ --- the window-integrated
fraction of species $i$ that decays before it is cleared --- so it enters
downstream as a reweighting of each isotope's contribution to the frozen
source, with no re-simulation. The five brain factors are $g_i \approx
0.4$--$0.5$: washout removes roughly $57\%$ of the counts, nearly uniformly
across isotopes. Two consequences follow, both measured on the two scanners.

\emph{Bias.} Because the survival is nearly isotope-independent, washout only
mildly reshapes the mix; it shifts the activity edge by
$\DR^{\mathrm{wo}} \approx +\SI{0.22}{mm}$ at zero delay (the surviving
$^{15}$O, which owns the distal edge, is preserved slightly more than
$^{11}$C), shrinking toward zero as a delayed start pre-depletes $^{15}$O. Like
every offset in the chain this is a deterministic constant, calibrated away by
the per-scanner reference simulation provided the reference itself includes
washout. Its uncertainty from the Mizuno parameters is $\pm\SI{0.02}{mm}$
(dominated by the medium and slow components; the $90\%$-uncertain fast
component is spent within seconds), well below $\sigma_R$. Washout therefore
introduces no range bias.

\emph{Precision.} The $\sim\!57\%$ count loss is the real cost. It inflates
$\sigma_R$ by $\approx 1.5\times$ --- the ordinary counting penalty
$1/\sqrt{0.43}$, since the removal is nearly uniform --- roughly independent of
the start time and the scanner (Table~\ref{tab:washout}), rising to
$\sim\!2\times$ only at \SI{300}{s}, where the doubly-cut (delay $\times$
washout) sample becomes count-starved. At \SI{1}{Gy} this takes $\sigma_R$ from
$\sim\!\SI{0.11}{}$ to $\sim\!\SI{0.16}{mm}$: a real but modest degradation,
recovered by the same $1/\sqrt{\text{dose}}$ lever as any count loss. (This
corrects an earlier reading from ten independent runs, whose small sample made
washout look free at zero delay; the figures here are from many thinned
realisations.)

\begin{table}[htb]
  \centering
  \caption{Washout precision cost: the $\sigma_R$ inflation (washed/nominal),
    per scanner and start time, from the thinned firm-up ($\pm 13\%$ per point).
    The edge shift itself calibrates away (text); this is what remains.}
  \label{tab:washout}
  \vspace{1mm}
  \begin{tabular}{lccccc}
    \toprule
    {$t_{\text{start}}$ [s]} & {0} & {60} & {120} & {180} & {300} \\
    \midrule
    BGO 195\,K & 1.34 & 1.43 & 1.76 & 1.16 & 2.02 \\
    CsI        & 1.48 & 1.74 & 1.57 & 1.54 & 1.87 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Outlook}
\label{sec:outlook}

Three directions follow from this round.

\paragraph{Scatter correction.} Recovering trues-level precision is worth
a factor $1.7$ (BGO) and $1.4$ (CsI) (Sec.~\ref{sec:tie}); at the present
precision, however, the other terms of the calibration dominate and the
working protocol needs no correction. The scatters-only reconstructions of
Fig.~\ref{fig:scatters} are the input for designing one when it becomes
worthwhile.

\paragraph{Isotope-resolved edge model.} The distal fall-off is a
superposition of per-isotope edges with known relative depths. A composite
erfc with simulation-fixed offsets and free amplitudes turns the edge
shape into a measurement of the isotope mix, and is the natural refinement
of Eq.~(\ref{eq:erfc}) for the start-time studies.

\paragraph{Geometry axis.} With the crystal contrast measured, the
remaining scanner axes --- ring length (sensitivity) and open geometries
(angular coverage) --- run through the identical analysis, which is
configuration-blind by construction.

\section*{Conclusions}

The whole-plane erfc fit of the $\beta^+$ depth profile, with $\Rfifty$ as
the range observable and the activity--dose offset calibrated per scanner
on the reference simulation, is a robust distal-edge estimator: its
calibration budget is measured to \SIrange{0.01}{0.04}{mm} per term on
both scanners, and the scattered coincidences bias it by no more than
\SI{0.04}{mm} without correction. The first technology round ends level:
BGO 195\,K and cryogenic CsI both measure the distal edge with
$\sigma_R = \SI{0.11}{mm}$ per \SI{1}{Gy} run at the working protocol ---
statistics against purity, at par. The trues-level precisions ($0.065$
vs.\ \SI{0.084}{mm}) state what a scatter correction would recover on each
scanner, making that correction the deciding question of the comparison.
On both scanners the precision scales as $1/\sqrt{\text{dose}}$ with a
dose-invariant calibration, so an exploratory \SI{0.1}{Gy} run verifies
the beam range to \SIrange{0.16}{0.23}{mm} before the clinical dose is
delivered. The precision also holds under a realistic acquisition start:
a \SIrange{2}{3}{min} delay after beam-off halves the statistics and
walks the calibration predictably (\SIrange{8}{11}{\micro m/s}), yet
$\sigma_R$ stays at \SI{0.11}{mm} --- the in-room measurement delivers
the zero-delay precision. Isotope washout, folded in as spatially uniform
brain clearance, adds no range bias --- its edge shift calibrates away like
any offset (parameter uncertainty $\pm\SI{0.02}{mm}$) --- but costs
$\approx 1.5\times$ in $\sigma_R$ ($\SI{0.11}{}\to\SI{0.16}{mm}$ at
\SI{1}{Gy}), the ordinary penalty for its $\sim\!57\%$ count loss.

\bibliographystyle{unsrtnat}
\bibliography{biblio}

\end{document}
